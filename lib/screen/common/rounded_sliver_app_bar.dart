import 'package:flutter/material.dart';

class RoundedSliverAppBar extends StatelessWidget {
  const RoundedSliverAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}